Add tool section button (contains the tools we will implement soon)
Add api tool mode! (first tool to add)
postman instpire but light weight version!

Api tool mode
* create api (name and port )
ex. 
    ProjectClone
    http://localhost:8080

* Automated feature.
    - Insert the code or details of api endpoints. then autoamtically create the endpoints of it.
    - faster testing