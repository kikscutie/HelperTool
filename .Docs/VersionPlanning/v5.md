Add tool section button (contains the tools we will implement soon)
Add api tool mode! (first tool to add)
postman instpire but light weight version!

Api tool mode
* create api (name and port )
ex. 
    ProjectClone
    http://localhost:8080

* Import swagger mode
    - Uses the swagger to fetch all endpoints and set in the api!
    - works liek swagger, easier