Add new feature : Secret holder!

User can put secrets there (just simple txt display , no fancy)
So theres a storage there just local. to be use json just it stays local but not getting remove.

Secret management:
CRUD of secrets
How to use:
Secret holder has password.
password will be set by the user. so every time to open this, needs password to open. just easy. no need database or fancy. sicne this is jsut offline app

Dispaly the list of secret key with its value.
Like:
name : JWT_PASSWORD
Api key : lkhaljkbnjkabadlmk

something like that.
sorted by alphabetical.


-------------------------------------------------------------------

Folder mode improvements:
Sort the folder in alphabetical order.

IF The selected folder has subfolders. Then scoped them also.

Example:
Select : Server (folder)
Mode : focus

Output:
All subfolder of server also included to be focus.

The same if the mode is ignore.





