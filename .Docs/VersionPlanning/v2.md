Improvements for my helper tool!

* Add filter for detecting.
    ex. 
        selected file extensions (html, css, js)
        this will only detect the files that is html,css and js. based on the selected file. basically just shows all files of that 
        if no selected. then default to all.
        
* Have refresh button to refresh the folder selected. 
    Why:
        * THe added files, codes, etc. in the in used folder of helper tool doesnt get updated. so i need to exit then reopen it just to get latest.

* Improve the searching.
h1 - Theres scenario where theres similar fodler name example, Backend/src/models and Frontend/src/models
if i search "models" it will; just show 2 models. and now i dont know which 1 is frontend or backend. so lets improve by having to show the full. like Backend/src/models something like that .

* Bonus:
    Since we have now detecting of file extensions. lets have ignore detection, when the same, the app has access to all files that a re not yet in ignore, so it has list of file extensions, then if i want that selcted to file extesnsion to be added in ignore list. then i can

